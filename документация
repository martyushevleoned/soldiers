-- запросы -------------------------------------------------------------------------------------------------------------
1) select * from get_private(sol_id)
Возвращает всю инфу о военнослужащем из таблицы "Военнослужащий" по айдишнику (sol_id)

2) select * from Военнослужащий
   inner join Подразделение
   on Военнослужащий."ID_подчинённого_подразделения" = Подразделение."ID_подразделения"
   where Военнослужащий."ID_военнослужащего" = sol_id
Возвращает список подчинённого подразделения по id военнослужащего
предполагается что оно может быть только одно

3) select * from get_personnel(union_id)
Возвращает список подразделений для которого данное является "большим подразделением"
т.е. список подразделений из которых состоит данное подразделение

4) select * from Военнослужащий where "ID_подразделения" = sol_id
это очевидно
Возвращает список военнослужащих подразделения


-- методы (func) -------------------------------------------------------------------------------------------------------
1) registration(m)
вызывается при получении сообщения от незарегестрированного пользователя
если в составе сообщения есть логин и пароль военнослужащего то:
    1. заносит инфу в registred_user
       т.е. в словарь который соотносит id чата и id военнослужащего
       config.registred_user[айди чата] = id военнослужащего

2) get_private(user_id)
Вызывается при нажатии на кнопку "личное дело"
обрабатывает запрос select * from get_private(sol_id)

3) get_surname(user_id)
тоже обрабатывает запрос select * from get_private(sol_id)
но используется уже чаще тк возвращает правильную форму обращения к военнослужащему

4) get_personnel(sol_id, commander_id)
в некотором роде рекурсивный метод
вызывается в меню выбора кому отдать приказ
в sol_id всегда передаётся id военнослужащего который нажал на кнопку
в commander_id передаётся id военнослужащего список чьего личного состава необходимо получить
ПЕРВАЯ ЧАСТЬ МЕТОДА
по sol_id узнаём является ли военнослужащий командиром подразделения
если нет то работа метода прекращается
если да то какого
ВТОРАЯ ЧАСТЬ МЕТОДА
тут генерируется клавиатура для выбора кому отдать приказ
если у commander_id есть подчинённые подразделения то клавиатура собирается по шаблону
    1. подразделение
    2. командир подразделения
если у commander_id нет подчинённых подразделений следственно он является командиром отделения
в клавиатуру собирается список всего отделения кроме него самого
ОБРАБОТКА НАЖАТИЯ
при нажатии на военнослужащего через calback проносится флаг (sol) и id военнослужащего которому будет отдан приказ
при нажатии на подразделение через calback проносится флаг (un) и id командира подразделения после чего метод вызывается снова

5) get_key(val)
иногда нужно получить id чата по id военнослужащего
для этого функция и существует

6) later_msg(bot)
метод работает с массивом config.later в котором хранятся приказы которые не были доведены
приказ не может быть доведён если военнослужащий не зарегестрирован
метод вызывается после успешной регистрации в боте и доводит приказ

